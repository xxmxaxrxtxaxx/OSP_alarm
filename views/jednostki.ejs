<!DOCTYPE html>
<html lang="en">

<%- include('naglowek', {});  %>


<body id="page-top">
    <!-- Navigation-->
    <%- include('menu', {});  %>
    <!-- nagłówek-->

    <header class="masthead2">
    </header>

    <section class="page-section bg-primary" id="members">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 text-center">
                    <h2 class="text-white mt-0">Członkowie!</h2>
                    <hr class="divider light my-4" />
                    <p class="text-white-50 mb-4">
                        Tutaj znajdą się informacje na temat członków jednostki.

                        <table class="table table-striped table-dark">
                            <thead>
                                <tr>
                                    
                                    <th scope="col">Nazwa jednostki</th>
                                    <th scope="col">Adres</th>
                                    <th scope="col"></th>
                           
                                </tr>
                            </thead>
                            <tbody>
                                <% for(var i=0;i<lista.length;i++){ %>                                
                                <tr>
                                    
                                    <td><%= lista[i].nazwa %></td>
                                    <td><%= lista[i].adres %></td>
                                    <td><a href="/strazacy/<%=lista[i].id%>">Edytuj strażaków</a><a href="#"> Edytuj jednostkę</a></td>
 
                                </tr>
                                <% }   %>

                            </tbody>
                        </table>

                    </p>
                    <a class="btn btn-light btn-xl js-scroll-trigger" href="#services">Dodaj użytkownika</a>
                    <a class="btn btn-light btn-xl js-scroll-trigger" href="#services">Usuń użytkownika</a>
                </div>
            </div>
        </div>
    </section>

</body>
</html>